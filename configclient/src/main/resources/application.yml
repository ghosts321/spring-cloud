server:
  port: 8009
spring:
  application:
    name: config-client

logging:
  level:
    root: info
    org:
      springframework:
        boot:
          autoconfigure: error
        web: info
      hibernate: error
      mybatis: info

